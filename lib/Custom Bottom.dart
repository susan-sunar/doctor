import 'package:flutter/material.dart';
class Button extends StatelessWidget {
  const Button({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("I am Susan"),
        centerTitle: true,
        backgroundColor: Colors.blue,
      ),
      body: Column(
        children: [

        ],

      ),
    );
  }
}
